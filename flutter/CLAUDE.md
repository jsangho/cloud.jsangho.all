# Flutter 행동 지침 (보조)

> 메인 규칙은 [루트 CLAUDE.md](../CLAUDE.md). 본 문서는 Flutter 작업 시에만 로드된다.

---

## 하네스 강제 실행

> 코드 작성 후 반드시 실행한다. 에러는 무시하지 않고 수정 후 완료 보고한다.

```bash
dart analyze          # 린트 (avoid_print 위반 시 에러)
dart format .         # 포매팅
```
