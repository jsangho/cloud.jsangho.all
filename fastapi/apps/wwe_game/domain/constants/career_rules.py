"""판정 수치와 근거 (하네스 §5).

**인기도가 나이보다 우선한다** — 2026-08-06 사용자 결정. 이 파일의 설계 축이다.

프로레슬링은 경기력만으로 굴러가지 않는다. 관중이 보고 싶어 하는 선수는 서른여덟에도
메인이벤트에 서고, 아무도 안 찾는 선수는 스물아홉에도 밀려난다. 그래서 나이 곡선은
독립 감점이 아니라 **인기도가 상쇄하는 감점**으로 들어간다.

세 곳에서 같은 원칙이 적용된다.
1. 경기 판정 — 인기도 가중치가 가장 크고(0.55), 나이 감점을 인기도가 최대 80% 지운다
2. 은퇴(부진) — 인기도가 1.5배로 계산돼, 인기 있는 노장은 밀려나지 않는다
3. 액트 — 인기도 60 이상이면 황혼 진입이 43세→47세로 미뤄진다 (`career_run.py`)
"""

from __future__ import annotations

from wwe_game.domain.value_objects.condition import InjuryGrade
from wwe_game.domain.value_objects.wrestler_identity import PlayStyle

# ── 주차 구조 ────────────────────────────────────────────────

PLE_INTERVAL_WEEKS = 13
"""대형 대회 주기. 연 4회다.

실제 단체는 연 10회 이상 열지만, 그때마다 진행이 멈추면 `weekly` 모드가 이벤트(320회)에
더해 PLE 120회로 클릭이 두 배가 된다. **멈춤은 "보고 싶은 것"일 때만 의미가 있다**는
기준으로 연 4회만 남겼다.
"""

WEEKLY_MATCH_CHANCE = 0.62
"""주간 TV에서 경기가 잡힐 확률. 나머지는 `PROMO`(대립 빌드업) 주차다.

**매주 싸우지 않는다**(스펙). 경기 없는 주차가 있어야 대립이 쌓일 시간이 생기고,
마모·부상도 현실적인 속도로 는다. PLE는 예외로 **항상 경기가 있다**.
"""

PROMO_MIC_GAIN_CHANCE = 0.18
PROMO_HEAT_GAIN = 6
"""빌드업 주차의 소득. 링에서 얻는 것 대신 말과 대립 열기를 얻는다."""

PLE_STOP_MAX_TICK_WEEKS = 13
"""이 이하의 틱을 쓰는 모드에서만 PLE에서 멈춘다.

`yearly`(52주 틱)는 한 틱이 이미 PLE 네 번을 품는다. 거기서 멈추면 모드를 고른 의미가
사라지므로 결과만 로그에 남기고 진행한다.
"""

# ── 경기 판정 ────────────────────────────────────────────────

WEIGHT_POPULARITY = 0.55
WEIGHT_IN_RING = 0.35
WEIGHT_MIC_WORK = 0.10
"""판정 가중치. **인기도가 가장 크다** — 이 파일의 대전제다.

경기력(`in_ring`)이 인기도보다 낮은 이유: 프로레슬링의 승패는 스포츠의 결과가 아니라
각본의 결과이고, 각본은 관중이 보고 싶어 하는 쪽으로 쓰인다.
"""

WIN_CHANCE_FLOOR = 0.20
WIN_CHANCE_CEILING = 0.80
"""승률 상하한. 완전히 지거나 완전히 이기는 커리어를 막는다."""

WIN_CHANCE_BASE = 0.20
WIN_CHANCE_SPAN = 0.60
"""`승률 = BASE + SPAN × 종합점수/100`. 종합점수 0이면 20%, 100이면 80%."""

DRAW_CHANCE = 0.06
"""무승부·무효 경기. 낮게 둔다 — 자주 나오면 결과가 흐려진다."""

# ── 나이 곡선 (인기도가 상쇄한다) ────────────────────────────

AGE_GROWTH_END = 25
AGE_PRIME_END = 34
AGE_DECLINE_END = 42
"""구간 경계. 20~25 성장기 · 26~34 전성기 · 35~42 하락기 · 43~50 베테랑."""

GROWTH_GAIN_MULTIPLIER = 1.15
"""성장기 스탯 상승 보너스. 감점이 아니라 보너스라 인기도 상쇄 대상이 아니다."""

DECLINE_PENALTY_PER_YEAR = 0.04
VETERAN_PENALTY_PER_YEAR = 0.08
AGE_PENALTY_CAP = 0.45
"""하락기 연 4%, 베테랑 연 8% 누적. 상한 45%.

상한이 없으면 50세에서 96%가 되어 인기도가 아무리 높아도 아무것도 못 한다. 나이가
커리어를 끝내는 것은 은퇴 조건의 몫이지 판정의 몫이 아니다.
"""

AGE_PENALTY_POPULARITY_OFFSET = 0.80
"""인기도가 나이 감점을 최대 80%까지 지운다.

`실효감점 = 나이감점 × (1 − 인기도/100 × 0.80)`

인기도 100인 50세는 45% 감점 중 9%만 받는다. 인기도 10이면 41%를 그대로 받는다.
**같은 나이라도 관중이 찾는지가 결과를 가른다** — 이 한 줄이 사용자 결정의 구현이다.
"""

VETERAN_MIC_BONUS = 8
"""베테랑 구간의 마이크웍 보정. 몸이 죽는 대신 말이 산다 (§3-D10)."""

# ── 부상 ─────────────────────────────────────────────────────

INJURY_BASE_CHANCE = 0.0025
"""주당 기본 부상 확률.

0.004로 잡았다가 낮췄다 — 마모가 쌓이면 배수가 붙어 후반에 부상이 몰리고, 30년을
완주하는 커리어가 하나도 안 나왔다(200판 시뮬).
"""

INJURY_WEAR_FACTOR = 2.5
"""마모가 부상 확률에 곱하는 배수. `wear` 100이면 3.5배가 된다."""

INJURY_PLE_MULTIPLIER = 1.8
"""대형 대회는 스팟이 크다."""

INJURY_STYLE_MULTIPLIER: dict[PlayStyle, float] = {
    PlayStyle.HIGH_FLYER: 1.6,
    PlayStyle.BRAWLER: 1.3,
    PlayStyle.POWERHOUSE: 1.1,
    PlayStyle.TECHNICIAN: 0.8,
    PlayStyle.SHOWMAN: 0.9,
}
"""스타일별 위험도. 하이플라이어가 가장 높고 테크니션이 가장 낮다 (§5)."""

INJURY_GRADE_WEIGHTS: tuple[tuple[InjuryGrade, int, int, int], ...] = (
    # (등급, 가중치, 회복 최소 주차, 회복 최대 주차)
    (InjuryGrade.MINOR, 72, 2, 6),
    (InjuryGrade.SERIOUS, 26, 10, 30),
    (InjuryGrade.CAREER_ENDING, 2, 0, 0),
)
"""부상이 났을 때의 등급 분포. 중대 부상은 2%뿐이지만 커리어를 끝낸다 (§3-D16)."""

INJURY_MATCH_PENALTY: dict[InjuryGrade, float] = {
    InjuryGrade.HEALTHY: 0.0,
    InjuryGrade.MINOR: 0.15,
    InjuryGrade.SERIOUS: 0.35,
    InjuryGrade.CAREER_ENDING: 1.0,
}
"""부상 중 경기 판정 감점. 부상 중에는 원칙적으로 쉬지만 무리하는 선택지가 있다."""

WEAR_GAIN_CHANCE_SHOW = 0.03
WEAR_GAIN_CHANCE_PLE = 0.50
WEAR_RECOVERY_PER_OFF_WEEK = 1
"""마모 수지 — **확률로 쌓는다.**

경기마다 +1 고정으로 두었더니 1560주 동안 회복(부상 중에만)의 열 배가 쌓여 마모가
100에 붙고 부상 악순환이 났다(200판 시뮬에서 전원 조기 종료). 주간 쇼는 3%, 대형
대회는 50%로 낮춰 30년에 걸쳐 완만히 오르게 했다. 스타일 배수가 여기에도 곱해진다.
"""

# ── 스탯 성장 ────────────────────────────────────────────────

GAIN_BASE_CHANCE = 0.055
"""주당 스탯 상승 시도 확률의 기준값.

0.075에서 낮췄다(사용자 요청 — 성장을 조금 더 어렵게). 100에 **닿을 수는 있되 쉽지는
않아야** 한다: 최고 스탯의 상위 10%는 여전히 100이지만 평균은 90→75로 내려온다.
그 결과 그랜드슬램이 68%→23%로 희소해진다.

**벨트 난도가 아니라 성장 속도를 조인 이유:** 난도를 올리면 상위 벨트만 마르고 하위
벨트는 그대로라 커리어의 모양이 안 바뀐다. 성장을 조이면 **잘 풀린 커리어만 정상에
닿는** 곡선이 되어 판마다 결과가 갈린다.
"""

GAIN_HEADROOM_EXPONENT = 0.65
"""체감 곡선의 완만함. `상승확률 = 기준값 × ((100 − 현재값)/100) ** 지수`.

지수 1.0(선형 체감)으로 두면 **상단에서 성장이 사실상 멈춘다** — 경기력 90에서 남은
여지가 0.1이라 확률이 10분의 1로 줄어, 30년을 꽉 채워도 100 근처에 못 간다.

0.65로 낮추면 90에서 0.22가 되어 두 배 넘게 살아난다. 여전히 체감하므로 100에
"붙어" 버리지는 않는다 — **잘 풀린 커리어만 90대 후반에 닿는 곡선**이 목표다.
"""

POPULARITY_GAIN_CHANCE = {
    "win": 0.14,
    "draw": 0.07,
    "loss": 0.05,
}
"""**패배도 인기도를 올린다.** 승리에만 붙였더니 인기도가 net 마이너스였다.

승률이 낮은 초반(28%)에 상승은 승리에만 붙고 하락은 패배에 붙으니, 기대값이 음수가
되어 커리어가 시작하자마자 잊혀졌다(200판 시뮬에서 평균 인기도 13). 프로레슬링에서
**잘 지는 것은 기술이고 명승부는 패자도 올린다** — 규칙을 현실에 맞추는 쪽이 옳았다.

세 값 모두 `(100 − 인기도)/100`이 곱해져 체감한다.
"""

NXT_POPULARITY_GAIN_MULTIPLIER = 2.4
"""NXT에 있는 동안 인기도가 오르는 속도 배수.

**작은 물에서는 빨리 눈에 띈다.** 메인 로스터와 같은 속도로 두었더니 콜업 임계값에
닿는 데 평균 21.5년이 걸리고 41%는 끝내 못 올라갔다(200판 시뮬) — NXT가 발판이 아니라
종착지가 됐다. 배수를 붙여 4~6년차에 올라오게 한다.

경기력에는 붙지 않는다. 기량은 무대 크기와 무관하게 는다.
"""

POPULARITY_DROP_CHANCE = 0.02
"""패배 시 인기도 하락 확률. 상승 기대값보다 확실히 낮게 둔다."""

ALIGNMENT_CLARITY_BONUS = 0.55
"""뚜렷한 캐릭터가 인기도 상승에 주는 배수. `1 + |성향|/100 × 0.55`.

**밋밋한 선수는 안 팔린다.** 성향 0은 관중이 응원할지 야유할지 모르는 상태이고,
±100은 색깔이 분명한 상태다. 확실한 악역은 확실한 선역만큼 돈이 된다 — 그래서
부호가 아니라 **절댓값**을 본다.

이 규칙이 없으면 `alignment`는 죽은 스탯이다. 덱에서 129번 쓰이는데 어떤 규칙도
읽지 않아, 힐턴을 하든 페이스로 남든 결과가 같았다(`backstage`와 같은 문제였다).
"""

POPULARITY_DECAY_CHANCE = 0.041
"""**인기는 가만히 있으면 식는다.** 매주 `인기도/100`에 비례해 하락을 굴린다.

0.055 → 0.030 → 0.041로 두 번 움직였다. 0.055에서는 균형점이 60대 중반이라 정상급도
월드 임계값(80)에 겨우 닿았고, 0.030에서는 반대로 거의 모두가 90대에 올라 그랜드슬램이
68%로 흔해졌다. 0.041이 **정상은 열려 있되 저절로 오르지는 않는** 지점이다.

부진 은퇴 경로도 이 값에서 살아난다(1% → 5%).

이게 없으면 인기도가 단조증가해서 300판을 돌려도 부진 은퇴가 한 번도 안 났다 —
은퇴 4조건 중 하나가 죽은 코드가 된다. 높이 오를수록 유지 비용이 커지는 쪽이
프로레슬링에도 맞다.

상승은 `(100 − 인기도)`에, 하락은 `인기도`에 비례하므로 둘이 만나는 지점에서 균형을
이룬다. 그 지점을 밀어올리는 것이 이벤트 선택의 몫이다(T5).
"""

CHAMPION_DECAY_RELIEF: dict[str, float] = {
    "world": 0.25,
    "secondary": 0.55,
    "tag": 0.75,
}
"""벨트를 들고 있는 동안 망각이 느려지는 배수. 가장 높은 계층 하나만 적용한다.

**챔피언은 계속 화제가 된다.** 대관 순간에만 인기도를 주고 곧 식게 두었더니 인기도가
평균 63에서 멈춰 월드 임계값(80)에 닿는 판이 200 중 12뿐이었다 — 그랜드슬램이 2%로
죽고 우선 규칙이 발동할 자리도 사라졌다.

이 규칙이 고리를 닫는다: **인기도가 기회를 만들고, 기회가 벨트를 만들고, 벨트가
인기도를 지킨다.** 방어전에 의미가 생기는 것도 여기서다.
"""

POPULARITY_DECAY_OFF_MULTIPLIER = 2.2
"""부상으로 쉬는 주차의 망각 배수. 안 보이면 더 빨리 잊힌다.

부상 잦은 커리어가 **부상 → 결장 → 망각 → 부진 은퇴**로 이어지는 경로를 만든다.
하이플라이어가 감수하는 위험이 실제로 커리어를 끝내는 방식이다.
"""

# ── 백스테이지 평판의 흐름 ───────────────────────────────────

BACKSTAGE_GAIN_CHANCE = 0.018
"""경기를 뛴 주차에 평판이 오를 확률. 나오는 것 자체가 신뢰다."""

BACKSTAGE_PROMO_GAIN_CHANCE = 0.015
"""빌드업 주차에도 조금 오른다 — 맡긴 일을 해낸 값이다."""

BACKSTAGE_OFF_DROP_CHANCE = 0.110
"""결장 주차에 평판이 깎일 확률. **쓸 수 없는 선수는 신뢰를 잃는다.**"""

BACKSTAGE_INJURY_PENALTY = -5
"""새로 다칠 때마다 즉시 깎이는 평판. 몸이 약하다는 평판은 실력과 별개로 쌓인다.

세 값을 함께 훑어 잡았다. 약하게(0.022/0.045/−3) 두면 평판이 26 아래로 안 내려가
**방출이 발동조차 못 하고**, 세게(0.014/0.18/−8) 두면 평균 평판이 25로 내려앉아
모두가 해고 직전이 된다. 지금 값은 방출 12%에 평균 평판 37 — 여유가 있되 위험이
실재한다.

**T5에서 다시 봐야 한다.** 지금 평판을 움직이는 것은 부상·결장뿐이라 사실상
"몸이 버티는가" 하나의 축이다. 덱에는 백스테이지를 건드리는 선택지가 206개 있고,
그게 붙으면 *처세*라는 두 번째 축이 생겨 분포가 달라진다.
"""

# ── 은퇴(부진) 판정 ──────────────────────────────────────────

DECLINE_CHECK_AGE = 35
"""이 나이부터 부진 은퇴를 판정한다 (§3-D16)."""

DECLINE_POPULARITY_WEIGHT = 1.5
DECLINE_IN_RING_WEIGHT = 0.5
DECLINE_WEAR_WEIGHT = 0.5
DECLINE_THRESHOLD = 45.0
"""`입지 = 인기도×1.5 + 경기력×0.5 − 마모×0.5`. 이 값이 45 미만이면 밀려난다.

**인기도가 경기력의 세 배로 계산된다.** 인기도 30만 있어도 45를 넘어 살아남고,
인기도 5짜리는 경기력 80이어도 밀려난다. 은퇴에서도 인기도가 우선이라는 뜻이다.
"""

DECLINE_GRACE_WEEKS = 26
"""입지가 임계 아래로 떨어져도 이 주차만큼은 버틴다.

한 주 삐끗해서 은퇴하면 30년 커리어가 사고로 끝난다. 반년 내내 아래에 머물러야
비로소 밀려난다.
"""

# ── 방출 판정 ────────────────────────────────────────────────

RELEASE_BACKSTAGE_FLOOR = 26
"""백스테이지 평판이 이 아래면 방출 위험권에 든다 (시작값 50).

**부진 은퇴와 다른 실패다.** 부진은 *관중이 안 찾아서* 밀려나는 것이고, 방출은
*단체가 못 참아서* 잘리는 것이다. 읽는 스탯도 다르다 — 부진은 인기도·경기력,
방출은 백스테이지 평판이다.
"""

RELEASE_POPULARITY_SHIELD = 55
"""이만큼 인기가 있으면 평판이 바닥이어도 자르지 않는다.

**돈이 되면 참아준다.** 프로레슬링에서 다루기 힘든 스타는 남고 다루기 힘든
미드카더는 잘린다. 인기도 우선이라는 이 게임의 대전제가 여기에도 적용된다.
"""

RELEASE_GRACE_WEEKS = 12
"""위험권에 머물러야 하는 주차. 부진(26주)보다 짧다 — **해고는 갑작스럽다.**"""

RELEASE_FLAG_GRACE_DIVISOR = 2
"""징계 대기 중이면 유예가 절반이 된다.

`suspension_pending`·`went_into_business_for_self`를 남긴 선택은 이미 한 번
선을 넘은 것이라, 다음 문제에서 단체의 인내가 짧다.
"""

RELEASE_TRIGGER_FLAGS: frozenset[str] = frozenset(
    {"suspension_pending", "went_into_business_for_self"}
)

RELEASE_MIN_AGE = 0
"""**나이 제한이 없다.** 부진 은퇴는 35세부터지만 방출은 신인에게도 온다 —
오히려 신인이 더 쉽게 잘린다. NXT에서 커리어가 끝나는 경로가 여기서 생긴다.
"""
